#include "../help_functions.h"
#include <vector>
#include <string>

using namespace std;

void sort(vector<string> & item)
{

}

int main()
{
    vector<string> words{};
    insert_unique(words);
    // print(words);
    // print_nice(words);
    return 0;
}